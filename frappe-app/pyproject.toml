[project]
name = "hira"
version = "0.0.1"
description = "Public storefront API for The Hira Store"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
